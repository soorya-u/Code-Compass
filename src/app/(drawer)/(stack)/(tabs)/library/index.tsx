import { SafeAreaView, ScrollView, Text } from "react-native";

export default function LibraryScreen() {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text className="text-black dark:text-white">Library Screen</Text>
      </ScrollView>
    </SafeAreaView>
  );
}
