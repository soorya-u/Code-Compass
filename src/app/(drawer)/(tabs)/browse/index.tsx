import { SafeAreaView, ScrollView, Text } from "react-native";

export default function BrowseScreen() {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text className="text-black dark:text-white">Browse Screen</Text>
      </ScrollView>
    </SafeAreaView>
  );
}
